def divider(a,b):
    try:
        return a / b
    except ZeroDivisionError:
        print('вы пытайтесь разделить на 0')
        return

a, b = (int(digit) for digit in input('ввидите два числа через пробел').split())
print(divider(a,b))
